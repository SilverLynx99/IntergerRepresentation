#include "Qfloat.h"

// Example
// 121234123412.12341235123512312335123
void ScanQfloat(Qfloat & x)
{
	// B1: Nhập vào là 1 chuỗi.
		// Có cần chuẩn hóa chuỗi ? Khoan hãy làm. Best user
	// B2: Xử lý chuỗi
		// Chuyển chuỗi về kiểu gì ?
		// Cần một hàm chuyển chuỗi gồm toàn số nguyên về dạng nhị phân
		// Cần một hàm chuyển phần phía sau dấu chấm về dạng nhị phân
		// Từ chuỗi nhận được, tính exp, cần một hàm chuyển số đó về dạng quá K
		// Từ chuỗi nhận được cần một hàm chuyển chuỗi đó vào KDL

	// 
}

void PrintQfloat(Qfloat x)
{

}
